apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: default

spec:
  type: NodePort
  selector:
    app: my-app

  ports:
    - port: 80          # external service port
      targetPort: 8080  # container port
      nodePort: 30007   # accessible port on node (30000–32767)